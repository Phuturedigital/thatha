# Dependencies (none required — this is a zero-build static site)
node_modules/
package-lock.json

# Vercel
.vercel

# Playwright output — regenerated on demand, ~34MB of PNGs. The audit scripts
# themselves are committed; their screenshots are not.
test/shots/

# Backups only — assets/ is committed (real THATHA brand + product imagery)
*.html.bak

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
